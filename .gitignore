# Netlify CLI artıkları (deploy state — repoya girmesin)
.netlify/

# Deploy klasörü (yalnız index.html + panel.html; kaynaktan kopyalanır)
publish/

# Kişisel Claude Code ayarları (commit'lenmez)
.claude/settings.local.json
